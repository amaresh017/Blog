<div>
  <h2>Update sub category<h2>
</div>
<%= form_for(@sub_category, :url => {:action => "update", :controller => "sub_categories", :id => @sub_category.id}, :html => {:method =>"put"}) do |f| %>
  <!-- Check for errors -->
	  <% if @sub_category.errors.any? %>
	    <% @sub_category.errors.full_messages.each do |msg| %>
	      <div class="alert alert-danger"><%= msg %></div>
	    <% end %>  
	  <% end %>
	<div class="form-group" >
  	<%= f.label :name %>
  	<%= f.text_field :name, class:'form-control' %>

  <br>
  <%= f.submit "Update", class:'btn btn-primary'%>
  <%= link_to 'Cancel', {:action => 'index', :controller => "sub_categories"}, class:'btn btn-default'%>	  
<% end %>	